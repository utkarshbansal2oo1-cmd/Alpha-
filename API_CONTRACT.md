See `docs/API_CONTRACT.md` — this is the canonical, current version.
(This root-level file is kept only because an earlier draft can't be deleted
from the workspace; don't edit it, edit the one in `docs/`.)
