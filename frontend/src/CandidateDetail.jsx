// Sprint 22: dead file -- the MVP-era standalone candidate detail view.
// Fully superseded by src/components/CandidateDrawer.jsx. Not imported
// anywhere. Left as an empty module (rather than deleted) because this
// sandbox cannot unlink pre-existing files.
export default null;
