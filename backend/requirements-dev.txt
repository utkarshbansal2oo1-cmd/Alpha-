# Test-only dependencies -- not needed to run the application, only to run
# the test suite. Installed separately from requirements.txt so production
# deployments don't pull in test tooling.
-r requirements.txt
pytest==8.3.3
respx==0.23.1
