"""GitHub Discovery Connector integration -- Sprint 20B.

Uses only GitHub's official REST API (https://docs.github.com/en/rest),
authenticated with a Personal Access Token. Mirrors the Greenhouse
integration's package layout (config.py / client.py / normalizer.py) so
the two connectors read the same way.
"""
