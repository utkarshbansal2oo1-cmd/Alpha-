/**
 * SUPERSEDED in Sprint 13 -- this file is no longer loaded by
 * background/service-worker.js and is kept only because the workspace
 * filesystem this project runs on does not allow deleting files once
 * written. Its functionality was replaced by the standalone, pluggable
 * Adapter SDK at ../../../adapter-sdk/ (see docs/ADAPTER_SDK.md), whose
 * built files are vendored into ../../vendor/adapter-sdk/ via
 * scripts/sync-adapter-sdk-to-extension.js and loaded per
 * vendor/adapter-sdk/manifest.json. Safe to ignore/remove in any
 * environment where file deletion is possible.
 */
